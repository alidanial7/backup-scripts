# PostgreSQL Docker Backup Configuration

# Container and Database Settings
# Name of the PostgreSQL container
BACKUPS_CONTAINER_NAME=postgres

# Name of the database to backup
BACKUPS_DATABASE_NAME=mydb

# Database user with backup privileges
BACKUPS_DATABASE_USER=postgres

# Backup Settings
# Directory to store backups (must be writable)
BACKUPS_BACKUP_PATH=./backup

# Log file path
BACKUPS_LOG_FILE=./logs/log.log

# Number of backup files to keep
BACKUPS_MAX_FILES=4

# Prefix for backup file names
BACKUPS_FILE_PREFIX=postgres_docker_backup

# Filesystem Backup Configuration

# Path to the directory to backup
FILESYSTEM_SOURCE_PATH=/path/to/source

# Directory to store filesystem backups
FILESYSTEM_BACKUP_PATH=./backup/filesystem

# Log file path for filesystem backups
FILESYSTEM_LOG_FILE=./logs/filesystem.log

# Number of filesystem backup files to keep
FILESYSTEM_MAX_FILES=5

# Prefix for filesystem backup file names
FILESYSTEM_FILE_PREFIX=filesystem_backup